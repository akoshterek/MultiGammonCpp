#ifndef __BGACTION_H
#define __BGACTION_H

#pragma once

#include <string>
#include "BgMove.h"
#include "BgBoard.h"

#include <exception>
#include <vector>
/*
class BgAction
{
public:
	BgAction(void);
	BgAction(std::vector<BgMove> moves)
	{
		m_moves = moves;
	}
	~BgAction(void);

	bool addMove (int point, int die, int who);
	const std::vector<BgMove>& getMoves() const {return m_moves;}
	size_t getNumMoves() const {return m_moves.size();}

private:
	BgBoard m_board;
	std::vector<BgMove> m_moves;
};
*/
#endif
